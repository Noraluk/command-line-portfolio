# hooks/

Custom React hooks. Named `useX`, one per file, named export.

Rules:
- Reusable logic only — if used once, inline it in the component (YAGNI).
- No JSX. Return values/handlers, not markup.

# styles/

Global CSS only: `index.css` (reset + base), `tokens.css` (CSS vars: colors, spacing, fonts).

Rules:
- Component-specific styles stay colocated with the component, not here.
- Use CSS custom properties from `tokens.css` — no hardcoded colors elsewhere.
